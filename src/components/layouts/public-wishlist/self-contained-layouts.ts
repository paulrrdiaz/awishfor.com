/** Layouts that own their full page composition via `PublicLayoutShell` rather than `PublicWishlistBody`. */
export const SELF_CONTAINED_LAYOUT_IDS = new Set([
	"collage-staggered",
	"split-image-right",
]);
