# Keep Sharp's Linux runtime in the lockfile/install tree when development
# happens on macOS but production runs on Vercel's Linux x64 platform.
supportedArchitectures:
  os:
    - current
    - linux
  cpu:
    - current
    - x64
  libc:
    - glibc
