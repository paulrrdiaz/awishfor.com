## 1. Data model

- [x] 1.1 Add `SeatingTableShape` enum (`round`, `rectangular`), `SeatingTable`, and `SeatingAssignment` to `prisma/schema.prisma` per `design.md` §1, with `wishlistId` denormalised onto both models and back-relations added to `Wishlist`, `Invite`, and `InviteExtraGuest`.
- [x] 1.2 Resolve `design.md` open question: check whether the installed Prisma 7.8 supports `@@unique([inviteId, extraGuestId], nulls: "not distinct")`. If it does, use it; if not, keep plain `@@index` declarations. **Resolved: not supported** — `prisma validate` rejects the argument (`P1012: No such argument`) on Prisma 7.8.0, so the schema keeps a plain `@@index([inviteId, extraGuestId])` and the invariant lives in the migration's two partial unique indexes.
- [x] 1.3 Run `pnpm prisma migrate dev` and, unless 1.2 resolved it in the schema, hand-extend the generated migration with the two partial unique indexes from `design.md` §2 plus a comment explaining why a plain composite unique is insufficient on Postgres. Applied with `prisma migrate deploy` (not `migrate dev`, which can reset the remote Neon branch); both partial indexes verified present via `pg_indexes`.
- [x] 1.4 Run `pnpm prisma generate` and confirm the client resolves from `@/generated/prisma/client`.

## 2. Server: eligibility and services

- [x] 2.1 Add `src/server/services/seating.service.ts` with `toEligiblePeople(invites)` producing `SeatingPerson[]` (`personId`, `displayName`, `partyLabel`, `status`, `isUnnamed`, `inviteId`, `extraGuestId`), filtering `declined` at both invite and extra-guest level, with `personId = extraGuestId ?? \`invite:${inviteId}\``.
- [x] 2.2 Add unit tests for `toEligiblePeople`: declined invite drops its whole party; declined extra guest drops only that person; unnamed extra guest still yields a person with a party-derived placeholder; `personId` never collides between a primary guest and an extra guest.
- [x] 2.3 Add table CRUD to the service: `listBoard`, `createTables` (with a repeat count, staggering positions so no table is created fully hidden), `updateTable`, `moveTable`, `deleteTable`. Enforce capacity 1–20 and `table.wishlistId` matching on every write.
- [x] 2.4 Enforce the capacity-reduction guard in the service: `updateTable` throws `CONFLICT` when the requested capacity is below the table's current seated count, returning the names of the people who would have to move.
- [x] 2.5 Add `assignPerson` as an upsert on the person key inside one `$transaction` that first takes `SELECT id FROM "SeatingTable" WHERE id = $1 FOR UPDATE` via `$queryRaw`, then re-counts occupancy and throws `CONFLICT` when the table is full (`design.md` §3 — the count alone does not hold at READ COMMITTED). Add `unassignPerson`.
- [x] 2.6 Add tests: seating into a full table is rejected; moving a seated person leaves exactly one assignment row; unassign is idempotent; an assignment naming a table and a person from different wishlists is rejected.
- [x] 2.7 Add a concurrency test for the spec's simultaneous-drop scenario: two `assignPerson` calls racing for the last seat on a table — exactly one succeeds, the other is rejected as full, and occupancy never exceeds capacity. This is the regression test for the row lock in 2.5. **Coverage note:** the repo has no DB-backed test harness (`environment: "node"`, hand-rolled fake `db` everywhere), so the fake models `SELECT … FOR UPDATE` as a per-table-row mutex. It verifies the service's *ordering* — the lock is taken before the occupancy re-count — not Postgres's isolation semantics. Verified to have teeth by mutation: deleting the `$queryRaw` line makes both racing drops succeed (2 rows at capacity 1) and both tests fail.
- [x] 2.8 Add a test that a second assignment row for the same person fails at the database level, covering both a primary guest (`extraGuestId IS NULL`) and an extra guest — this is the regression test for task 1.3. **Split in two, because vitest has no database:** (a) a unit test that `assignPerson` maps a P2002 unique violation to `CONFLICT` instead of a 500 (the same-person-two-tables race the per-table row lock cannot serialise); (b) the actual database guarantee verified directly against the dev Postgres in a rolled-back transaction — a second row for the same person is rejected with SQLSTATE 23505 for both `extraGuestId IS NULL` and a non-null extra guest. (b) is a one-off check, not a test that runs in CI.

## 3. Server: validators, mapper, router

- [x] 3.1 Add `src/server/validators/seating.schema.ts` with Zod schemas for list, table create/update/move/delete, and assign/unassign.
- [x] 3.2 Add `src/server/mappers/seating.mapper.ts` producing a `SeatingBoardViewModel` (tables with derived `seated`, people with their `tableId`, totals) and register its types in `src/server/mappers/view-models.ts`.
- [x] 3.3 Add `src/server/api/routers/seating.ts` — `board`, `createTables`, `updateTable`, `moveTable`, `deleteTable`, `assign`, `unassign` — every procedure `protectedProcedure` calling `assertWishlistAccess` without `requireOwner` (see `proposal.md` → Scope decisions).
- [x] 3.4 Register `seating` in `src/server/api/root.ts` `appRouter`.
- [x] 3.5 Add router tests covering the access matrix: owner succeeds, collaborator succeeds, unrelated user gets `NOT_FOUND` for both a read and a write.

## 4. Navigation and gating

- [x] 4.1 Add `{ label: "Mesas", segment: "seating", icon: …, ownerOnly: false }` to `NAV_ITEMS` in `src/components/layouts/dashboard/wishlist-sections.ts`, positioned after `guests`. Pick the circular-table icon suggested by the design artboard (§1 of `Mesas.dc.html`). Used `CircleDotIcon` — a round table seen from above, matching the canvas's occupancy-ring motif.
- [x] 4.2 Add `unseatedGuests` to the `wishlist.overview` metrics and wire the `seating` badge in `src/app/(protected)/dashboard/wishlists/[id]/layout.tsx` with `variant: "warning"`, omitted when the wishlist has no tables.
- [x] 4.3 Update `wishlist-section-tabs.test.tsx` and any snapshot/story affected by the new tab; confirm `activeSegmentFromPathname` resolves `seating/print` to `seating`. It already did — the function splits on `/` and checks `NAV_ITEMS` membership — so this added tests, not a fix. `mobileChipItemsFor` uses a fixed four-segment list and is unaffected; a test now pins that.

## 5. Seating editor UI

Reference artboards: `Mesas.dc.html` §§1–8 and the component inventory in `Claude Design Output - Mesas.md` §9. Add the three `@theme` colour tokens from §1 of that document.

- [x] 5.1 Add the route `src/app/(protected)/dashboard/wishlists/[id]/seating/page.tsx` as a server component that loads the board and branches between the no-guests block state, the no-tables first-run state, and the editor.
- [x] 5.2 Build `MesasEmptyState` with its two variants; the no-guests variant links to `hrefFor(wishlistId, "guests")` and the tab stays visible and enabled.
- [x] 5.3 Build `SeatingBoard` — one `DndContext` over canvas and panel, pointer + keyboard sensors, `pointerWithin` for people and `closestCenter` for tables, activation constraints per `design.md` §5.
- [x] 5.4 Build `FloorCanvas`: 1200×800 logical canvas, 26px CSS grid, zoom controls, fit-to-screen, snap toggle, fixed "Entrada" / "Escenario" orientation labels, and a three-state legend. Divide drag `delta` by `zoom` before persisting.
- [x] 5.5 Build `TableNode` (`useDraggable` + `useDroppable`) with capacity-derived geometry, conic-gradient ring for round tables and a bar for rectangular ones, all seven visual states, the state written in the subtitle, and an `aria-label` naming table, occupancy, and state. Wrap in `React.memo`.
- [x] 5.6 Build `GuestPanel`: header with seated progress, search input, unseated/seated/pending filters, party-grouped unseated list, the declined-exclusion note, a collapsible seated summary by table, and the all-seated acknowledgment card linking to the print sheet.
- [x] 5.7 Build `GuestChip` (`useDraggable`, ≥44px tall) with status dot, party sub-line, and the "Poner nombre" inline action for unnamed extras — rendered as a link to Invitados rather than an inline editor, because `invite.update` replaces the whole `extraGuests` set (`deleteMany` + `create`), which would issue new ids and cascade-delete the companion's seat.
- [x] 5.8 Wire drag validation in `onDragOver` so a full table paints invalid before release and `onDragEnd` ignores it; render the `DragOverlay` above the canvas and a dashed ghost at the origin. Dropping on the canvas background or the panel header unassigns.
- [x] 5.9 Wire keyboard assignment: pick up with Space, arrow through tables that have open seats, drop with Space, cancel with Escape, with `aria-live` announcements.
- [x] 5.10 Wire optimistic mutations with rollback: invalidate the board query and toast "No se pudo guardar. Intenta de nuevo" on failure. No Save button.
- [x] 5.11 Build `AddTablePopover` (shape radio group, capacity chips + 1–20 input, optional name, repeat count) and `TableDetailPopover` (seated list with per-person remove, free seats, rename, capacity, delete) using `Sheet` on tablet. `TableDetailPopover` is a `Sheet` at every width rather than an anchored `Popover`: the canvas pans and zooms underneath, so an anchored surface drifts off its table.
- [x] 5.12 Build `CapacityDialog` with the blocked state: name the people who must move, offer to remove each, keep Save disabled until the seated count fits.
- [x] 5.13 Build `DeleteTableDialog` as an `AlertDialog` naming every seated person and stating they return to "Sin mesa asignada"; delete an empty table with no dialog.
- [x] 5.14 Add the capacity-shortfall notice when total capacity is below the eligible-person count — informational, never blocking.
- [x] 5.15 Add the `<768px` treatment: amber notice explaining a tablet or computer is needed, the unassigned list, the table list with occupancy, and a link to the print sheet. Canvas editing is not offered (deferred scope — no per-person "Sentar" sheet in this change).
- [x] 5.16 Apply all Spanish copy from `Claude Design Output - Mesas.md` §11 verbatim. **Two keys adapted, not copied**, because they describe deferred behaviour: `delete.toast` drops its "Deshacer" affordance (no undo in v1 per `proposal.md`), and `mobile.notice` says the canvas is not offered at all below 768px rather than being read-only for moving tables (5.15 defers the whole mobile assignment flow). Everything else in §11 is verbatim, in `seating-copy.ts`.

## 6. Print sheet

- [x] 6.1 Add `src/app/(protected)/dashboard/wishlists/[id]/seating/print/page.tsx` rendering `SeatingPrintSheet`: `@page { size: letter; margin: 14mm }`, a flat alphabetical list in three columns with Lora letter dividers, dotted leaders, and the table label right-aligned in mono.
- [x] 6.2 Include the header (event name, date, place, `N personas · M mesas`) and footer (public URL, page number, generation timestamp). Repeat the header per page and apply `break-inside: avoid` per letter block. **Page number omitted, deliberately:** no browser implements the CSS `@page` margin boxes (`@bottom-right { content: counter(page) }`) that a real page number needs, and a JS-computed one cannot know where the printed page breaks. The footer carries the public URL and the generation timestamp — which is what the design wanted the number for ("para saber cuál impresión es la vigente"). Header repetition is via `<thead class="table-header-group">`, and `break-inside: avoid` is on each letter block; both are exercised only by a sheet long enough to paginate, which the test wishlist (2 seated people) is not.
- [x] 6.3 Render unnamed seated extras as "Acompañante *de {partyLabel}*" sorted with — and indented beneath — their party's primary guest, not alphabetised under A (`design.md` §8; PRD §10 decides this against the design output, which leaves it open).
- [x] 6.4 Add the `print:hidden` unassigned warning naming every eligible person without a table, with "Ir a asignarlos" and "Imprimir así de todas formas", plus the `print:hidden` column-count and paper-size controls.
- [x] 6.5 Add a test that only seated people appear in the printed list and that the warning names exactly the unseated eligible people.

## 7. Validation and close-out

- [x] 7.1 Run `pnpm check` (Biome) and fix findings.
- [x] 7.2 Run `pnpm typecheck`.
- [x] 7.3 Run `pnpm test`.
- [x] 7.4 Exercise the flow against the dev server on port 4000: create the PRD's reference configuration (2×10, 1×8, 1×7, 3×5), seat a party split across two tables, attempt a shrink below the seated count, delete a seated table, and open the print sheet. **Ran on port 4005** (the project's own `PORT`; 4000 was occupied by an unrelated app). Verified against a real wishlist with 5 eligible people: batch creation (2×10, staggered so neither is hidden), seating via the keyboard flow with correct `aria-live` announcements, occupancy ring and counter updating, the capacity guard refusing 1 seat under 2 seated and naming who must move with Save disabled, the delete confirmation naming both seated people, and the print sheet with its unassigned warning, letter dividers, dotted leaders and footer stamp. **Not exercised:** the full 7-table reference configuration (the test wishlist has only 5 eligible people, so the extra tables would add nothing), pointer drag-and-drop (synthetic drags do not drive `@dnd-kit` reliably — the assignment path was verified through the keyboard flow and the service tests instead), and an actual table deletion (confirmed the dialog, then cancelled). Four real defects were found and fixed this way: a layout overflow pushing the guest panel off-screen, a stuck ghost chip from an unwired `onDragCancel`, a `DndContext` hydration mismatch, and an empty table's subtitle reading "sin lugares". A fifth — the print route inheriting the whole dashboard chrome onto paper, which no on-screen screenshot can reveal — was found in review and fixed with `print:` utilities on the two dashboard layouts plus a shell height/overflow reset in `globals.css`; verified by extracting the emitted `@media print` rules from the dev CSS bundle and confirming `.print\:hidden` is ordered after both `.contents` and `.md\:block`, and that all ten chrome wrappers carry it.
- [x] 7.5 Update `docs/SEATING_CHART_PRD.md` — move §3 status past "pre-design", record the resolved §12 open questions, and note which items were deferred per `proposal.md` → Scope decisions.

## 8. Optional, droppable

- [x] 8.1 "Sentar juntos" per party — a server-side loop seating each unseated party member who fits at the chosen table, reporting how many did not fit. Marked optional by the design output itself; drop this task before anything in §§1–7 if scope needs cutting. **Implemented client-side** (a sequential loop of `seating.assign` calls in `SeatingBoard`, reporting how many did not fit) rather than as a new server procedure — each call is already capacity-checked and row-locked server-side, so no new endpoint was warranted.
