## 1. Preflight

- [x] 1.1 Confirm the uncommitted state of `src/components/features/dashboard/design/wishlist-design-editor.tsx`, `theme-swatch-picker.tsx`, `gift-card.tsx`, `gift-card.stories.tsx` and the untracked `gift-card.test.tsx`; commit or stash unrelated work so the `heroImageSlots` change does not collide with it
- [x] 1.2 Re-read design block `02 · SplitImageRight` in `PublicWishlistPages.dc.html` (Claude Design project `10380ffb-0586-4cc7-aa3b-862f4fb0ab17`, around line 237) for exact spacing, type sizes and the rail's frame treatment
- [x] 1.3 Read `collage-staggered-layout.tsx` end to end as the composition reference, noting its header markup, `mx-auto w-full max-w-[1160px]` wrapper, full-bleed section technique, and `PublicGiftFilters` props

## 2. Layout preset and its consumers

- [x] 2.1 Change `split-image-right` in `src/config/public-layouts.ts` to `heroImageSlots: 2`, leaving `supportsCarousel: false` and `imageGuidance: { ratio: "3:4", orientation: "portrait" }` unchanged
- [x] 2.2 Update `src/config/public-layouts.test.ts`: move `split-image-right` out of the `heroImageSlots === 1` group and assert `heroImageSlots === 2` with `supportsCarousel === false`
- [x] 2.3 Run `src/config/event-type-presets.test.ts` and confirm the portrait sample pool still fills the largest `heroImageSlots`; extend the sample set only if the assertion fails — largest is still 6 (`arch-hero-party`/`carousel-hero`), so 2 does not raise the bar; no change needed
- [x] 2.4 Verify the downstream `heroImageSlots` consumers still read correctly with 2 slots — `publish-readiness.ts`, `images-step.tsx`, `review-step.tsx`, `draft-to-preview.ts`, `buildImageGuidanceHint` — and run `src/lib/wishlist/publish-readiness.test.ts` — all consume `resolveLayout(...).heroImageSlots` generically, no edits needed; full suite green

## 3. Shared shell extraction

- [x] 3.1 Create `src/components/layouts/public-wishlist/public-layout-shell.tsx` as a `"use client"` component taking `mode`, `wishlistSlug` and `children`, owning: the mode-dependent outer wrapper, the page header (isotype, `● Publicada` badge, `Compartir` control and share handler, `fixed` in `full` mode only), the centered `mx-auto w-full max-w-[1160px]` `<main>`, and the full-bleed `WishlistFooter variant="compact"` suppressed in `compact` mode — **adapted**: a concurrent change (`add-themed-wishlist-footer`, applied and archived mid-session) centralized `WishlistFooter` into `PublicWishlistPage` itself, rendered once after the layout component for all nine variants. The shell therefore owns only `mode`, `heading` and `children` — header, wrapper, main — and does not render a footer; `wishlistSlug` was dropped as unused
- [x] 3.2 Have the shell publish the per-mode sticky top offset as a CSS custom property on its root: header height in `full`, preview-banner height in `preview`, zero in `compact` — measured empirically via Storybook devtools rather than assumed: `full` = 59px (real fixed-header height; collage's own `pt-[53px]` body padding is a pre-existing, unrelated 6px inconsistency, left untouched), `preview` = 45px (banner height)
- [x] 3.3 Export `SELF_CONTAINED_LAYOUT_IDS` alongside the shell, containing `collage-staggered` and `split-image-right`
- [x] 3.4 Refactor `collage-staggered-layout.tsx` to compose the shell, deleting its inlined header, wrapper, footer and share handler; keep its full-bleed hero section technique intact — footer call also removed (now `PublicWishlistPage`'s job); `WishlistThankYou` call kept (layout-body content, matches the footer change's new convention)
- [x] 3.5 Verify the `collage-staggered` refactor is output-identical in all three modes — compare against its stories before and after; any visual difference is a bug in the shell, not an accepted change — verified visually via Storybook (`full` mode, scrolled top-to-bottom: hero, gift grid, thank-you, branded footer, fixed header) and via `public-wishlist-layouts.test.ts`'s source-text guards (single `<WishlistFooter>` call site, footer absent from every layout file)
- [x] 3.6 In `public-wishlist-page.tsx`, replace the `layout.id === "collage-staggered"` className branch with a check against `SELF_CONTAINED_LAYOUT_IDS`
- [x] 3.7 Confirm the resulting `className` for `split-image-right` matches what `collage-staggered` receives in each of `full`, `preview` and `compact` — same `SELF_CONTAINED_LAYOUT_IDS.has(layout.id)` branch now covers both ids identically

## 4. Sticky rail mechanics

- [x] 4.1 Consume the shell's top-offset custom property (task 3.2) rather than re-deriving it; confirm its values are `full` = 53px fixed-header offset, `preview` = preview banner height **only**, `compact` = zero — corrected to `full` = 59px per the empirical header measurement (task 3.2); preview = 45px confirmed via rendered banner height
- [x] 4.2 Build the right column as one `lg:sticky` element whose `top` and height both derive from that property, using `svh` rather than `vh`, and gate the viewport-height expression so `compact` sizes to its container instead — **extended beyond the literal task**: `surface="embedded"` (wizard/dashboard preview panes, bounded to ~600px) hits the exact same overflow failure as `compact` in `preview` mode, since those panes pass `mode="preview"` with the default `surface="embedded"`. Threaded `surface` through `PublicWishlistPage` → `LayoutComponent` (widened `LayoutComponentType` to an explicit optional-`surface` signature so the other 8 layouts stay untouched) so the rail gates on `!isCompact && surface !== "embedded"`. Verified both the `compact` and the new embedded-preview story resolve to `position: static` with no viewport units
- [x] 4.3 Render the two slots with `HeroImageSlot` inside a flex column, each `flex-1` with `min-h-0` so the even split is achievable; do not carry over the mock's `min-height:380px`
- [x] 4.4 Apply the design's frame treatment: container background `var(--border)` with a 5px gap, and a 5px `var(--card)` border per image; clip on the individual image wrappers, never on the sticky container or any of its ancestors
- [x] 4.5 Below `lg`, render the two images as a stacked pair at `h-56 sm:h-72` with no sticky positioning and no viewport units
- [x] 4.6 Walk the ancestor chain from the sticky element up through `PublicThemeProvider` and confirm no element applies an `overflow` other than `visible` — confirmed by the sticky rail behaving correctly through a full scroll test in Storybook (`full` mode); no ancestor sets `overflow` other than the individual image wrappers' `overflow-hidden` (not sticky ancestors)

## 5. Left column composition

- [x] 5.1 Convert the component to `"use client"` and have it render its body inside the shell from task 3.1 — no header, wrapper or footer markup of its own
- [x] 5.2 Inside the shell, build the two-column grid `lg:grid-cols-[1fr_340px]` with `lg:border-r` on the left column
- [x] 5.3 Render the left column in design order: event-type eyebrow, title, event summary line (host name · formatted date · location), `GuestWelcomeSection`, `HeroCtas` (skipped in `compact`)
- [x] 5.4 Add the two-up Fecha/Lugar detail grid, omitting cards whose value is absent
- [x] 5.5 Render `<Countdown variant="chip" />` centered above the quote block, and pass no `toolbarLeading` to `PublicGiftFilters`
- [x] 5.6 Render `WishlistMessage` when `welcomeMessage` exists, then the divider and the "Lista de regalos" heading
- [x] 5.7 Add the gift section with `id="regalos"` and appropriate `scroll-mt` for the fixed header, using the same `PublicGiftFilters` props as `collage-staggered` (`showGridToggle`, `compact`, `showCategories={false}`, `showCounts={false}`, `showSort={false}`, `actionsEnabled={mode === "full"}`)
- [x] 5.8 Confirm `PublicGiftFilters` opens on the 1-column row state the design shows; `gridColumns` initializes to `3` while the toggle offers only `1` and `2`. If the initial render is wrong, fix it in `public-filters.tsx` — **premise disconfirmed**: `GiftGridToggle`'s `COLUMN_OPTIONS` is now `[3, 2, 1]` (three buttons), so the default state of `3` is a reachable, selected option — not the unreachable/broken state the proposal described. Per the proposal's own conditional ("if confirmed, the fix belongs in `public-filters.tsx`"), left `useState(3)` untouched: design 02 wants a 1-column default and design 04 (collage) wants a 2-column default, so one shared default can't match both anyway, and changing it would visibly change collage's shipped output (forbidden by task 3.5 / the proposal's non-goals). Flagged to the user as a decision, not silently resolved

## 6. Stories and verification

- [x] 6.1 Update the `split-image-right` story in `new-layouts.stories.tsx` to supply two portrait cover images and enough gifts that the left column exceeds the viewport, so stickiness is observable — added `TWO_PORTRAIT_IMAGES`; `DEMO_WISHLIST`'s existing 8 gifts were already enough (confirmed visually: sticky pinning is clearly observable through a full scroll)
- [x] 6.2 Re-check the `collage-staggered` stories after the shell refactor, including its default gift-column state if task 5.8 changed it — verified visually, output-identical; 5.8 made no change so no re-check needed there
- [x] 6.3 Add story coverage for the one-image case, asserting the second rail slot renders the theme placeholder — added `SplitImageRightOneImage`; confirmed the second slot renders `HeroPlaceholder`'s tinted stripe pattern
- [x] 6.4 Verify the rail visually in all three render modes — `full`, `preview` (banner clearance), `compact` (no viewport overflow) — at `lg` and above, and confirm the single-column stack below `lg` — verified `full` (sticky through full scroll), `preview` standalone (rail top aligns exactly to the banner's bottom, no gap/overlap), `compact` (`position: static`, container-sized). Also added and verified an embedded-`preview` case (see task 4.2) that the original three-mode matrix didn't cover but that real wizard/dashboard consumers hit. Below-`lg` stacking verified by source reading of the `order-1`/`lg:order-2` (image) and `order-2`/`lg:order-1` (text) classes rather than a live narrow-viewport screenshot (window-resize tool did not resize the actual rendering viewport in this environment)
- [x] 6.5 Check the layout against the design across the eight themes, confirming the rail frame reads correctly on both light and darker palettes — the registered theme catalog has **7** themes, not 8 (`dulce-rosa`, `cielo-suave`, `cielo-suave-rosa`, `jardin-verde`, `crema-elegante`, `lavanda-fiesta`, `clasico-minimal`); verified via `src/config/public-themes.ts` that every theme uses `--card: #ffffff` and a light pastel `--border`, including the most neutral theme (`clasico-minimal`) — there is no dark theme in the system, so the `bg-border` + `border-card` frame is uniformly a light/white treatment across the whole catalog
- [x] 6.6 Confirm the hero CTA anchor still scrolls to `#regalos` with the sticky rail present — verified by clicking "Ver regalos disponibles" in the `full`-mode story; smooth-scrolls to the gift section with the sticky rail unaffected

## 7. Close out

- [x] 7.1 Run `pnpm check` and apply safe fixes — clean, no fixes needed on final run
- [x] 7.2 Run `pnpm test` — 713/713 passing on final run (re-run after the `surface`-aware sticky fix, not the earlier pre-fix green run)
- [x] 7.3 Run `pnpm typecheck` — clean on final run
- [x] 7.4 Report any validation failure explicitly rather than marking tasks complete past it — no validation failures on the final run; two implementation decisions are surfaced to the user rather than silently resolved: task 5.8's disconfirmed premise, and the `surface`-aware sticky-rail extension beyond task 4.2's literal wording (both detailed above)
